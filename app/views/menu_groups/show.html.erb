<% @menu_items.each do |item| %>
  <%= item.name%>
  <%= item.description %>
  <%= item.price %>
  <img src="<%= item.image %>" alt="">
<% end %>

<%= render 'menu_items/form', menu_item: @menu_item%>
